@model IEnumerable<BrandModel>

@{
    ViewData["title"] = "Brand List";
}
<a asp-action="Create" asp-controller="Brand">Add Brand</a>
<h4>List-Category</h4>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Brand Name</th>
            <th scope="col">Brand Description</th>
            <th scope="col">Brand Slug</th>
            <th scope="col">Brand Status</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td>@Html.Raw(@item.Description)</td>
                <td>@item.Slug</td>
                <td>
                    @if(@item.Status == 1){
                    <span class="text text-success">Show</span>
                    }else{
                    <span class="text text-danger">Hide</span>
                    }
                </td>
                <td>
                    <a asp-action="Edit" class="btn btn-primary btn-sm" asp-controller="Brand" asp-route-Id="@item.Id">Edit</a>
                    <a asp-action="Delete" class="btn btn-danger btn-sm" id="confirmDeletion" asp-controller="Brand" asp-route-Id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>